import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2.2_Manejo_de_listas_frameworks',
  webDir: 'dist'
};

export default config;
